<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing here is worth carrying to a new device. The session belongs to Clerk's
  own store, and everything else the app shows is server state that a sign-in
  fetches again. Backing up a stale copy of it would be a way to restore
  somebody else's account onto a phone.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>

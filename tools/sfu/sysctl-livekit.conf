# pqp SFU box. Installed by tools/sfu/install.sh as /etc/sysctl.d/90-livekit.conf.
# LiveKit requests a 16 MB buffer per UDP mux socket; the default rmem_max
# (212992) clamps it and LiveKit warns on every boot. 25 MB leaves headroom.
net.core.rmem_max = 26214400
net.core.wmem_max = 26214400
